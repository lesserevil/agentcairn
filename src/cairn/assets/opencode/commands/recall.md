---
description: Search your agentcairn memory vault for relevant notes
---
Search my agentcairn memory vault for notes relevant to the following query and summarise the most useful findings:

$ARGUMENTS

Run the search with: `!cairn recall "$ARGUMENTS" --json --k 5`

If $ARGUMENTS is empty, ask me what to search for before running the command.
