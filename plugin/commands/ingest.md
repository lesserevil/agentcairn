---
description: Distill recent sessions into the vault now.
---
Run `uvx --from agentcairn cairn sweep --vault "${CAIRN_VAULT:-$HOME/agentcairn}"` to ingest and reindex recent sessions (the index is derived from the vault), then report how many memories were written.
