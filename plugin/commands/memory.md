---
description: Show agentcairn memory health (vault + index status).
---
Run `uvx --from agentcairn cairn doctor` and `uvx --from agentcairn cairn index-status` (both take `--vault`; the index is derived from it) and summarize the vault location, note/chunk counts, and any health warnings.
