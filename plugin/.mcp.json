{
  "mcpServers": {
    "agentcairn": {
      "command": "uvx",
      "args": ["agentcairn"],
      "env": {
        "CAIRN_VAULT": "${user_config.vault_path}",
        "CAIRN_INDEX": "${user_config.index_path}"
      }
    }
  }
}
